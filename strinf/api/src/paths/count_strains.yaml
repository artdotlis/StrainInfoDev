get:
    operationId: cnt-str
    summary: Count of all strains
    description: >-
        Retrieves the count of all strains stored in the database.
    tags:
        - Statistic
    responses:
        '200':
            description: OK
            content:
                application/json:
                    schema:
                        $ref: ../components/content/stat_cnt.yaml
