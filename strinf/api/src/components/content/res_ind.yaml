type: object
required:
    - match
    - exact
properties:
    match:
        type: array
        items:
            $ref: './search/ind_elm.yaml'
    exact:
        type: array
        items:
            $ref: './search/ind_elm.yaml'
