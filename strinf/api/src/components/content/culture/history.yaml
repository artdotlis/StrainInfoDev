type: array
items:
    type: object
    required: [encoded, data_source]
    properties:
        data_source:
            $ref: './data_source.yaml'
        encoded:
            type: string
            minLength: 1
