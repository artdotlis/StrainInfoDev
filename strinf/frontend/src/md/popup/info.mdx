import { UIApiCon } from '@strinf/ts/constants/api/ui_api';

### Welcome to StrainInfo

StrainInfo is a service developed to provide a resolution of microbial strain identifiers.

We are collecting exclusively anonymized data, more information can be found in
our <a href={UIApiCon.impPrivacy} style="color: var(--blue-color);">privacy policy</a>.

If you find any bugs or have feature suggestions, feel free
to <a href={UIApiCon.contact} style="color: var(--blue-color);">contact us</a>. {props.env}
